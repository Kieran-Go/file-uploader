<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Uploader - Sign Up</title>
</head>
<body>
    <h1>Sign Up:</h1>
    <form action="sign-up" method="POST">
        <label for="name">User name:</label>
        <input id="name" name="name" type="text" value="<%= data?.first_name || '' %>" required>

        <label for="email">Email:</label>
        <input id="email" name="email" type="email" value="<%= data?.email || '' %>" required>

        <label for="password">password:</label>
        <input id="password" name="password" type="password" value="<%= data?.password || '' %>" required>

        <button type="submit">Sign up</button>
    </form>
</body>
</html>